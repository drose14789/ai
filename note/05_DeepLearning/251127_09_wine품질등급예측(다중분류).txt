red wine 품질 등급 예측
1.데이터셋 확보 & 전처리 독립변수와 타겟변수 분리 -> 독립변수 스케일조정(StandardScaler) -> 
  타겟변수원핫인코딩(get_dummies) -> 훈련셋과 테스트셋분리(train_test_split)
2. 모델 구성(입력11, 출력6)
3. 모델학습과정 설정(다중분류로 설정)
4. 모델 학습(callbacks 이용)
5. 모델 평가 - 학습과정 시각화, 평가(테스트셋), 교차표(혼동행렬)
6. 모델 저장/사용 